git config --global user.name "bishkekpochta312-lab"
git config --global user.email "bishkek.pochta312@gmail.com"

git init
git remote add origin https://github.com/bishkekpochta312-lab/oop-63-2

git status | состояние проекта
git add .   | скопировать все в буфер обмена
git commit -m "lesson1"
git commit -m "update lesson1"
git branch
git push origin master
